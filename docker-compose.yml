services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: subtube
      POSTGRES_USER: subtube
      POSTGRES_PASSWORD: subtube
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
